# Camera Color Mixer

A live browser camera feed with real-time color remix controls.

Open the GitHub Pages URL, press **Start camera**, and allow camera access. The app runs entirely in the browser; no camera frames are uploaded.
